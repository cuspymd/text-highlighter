{
  "name": "text-highlighter",
  "version": "1.0.0",
  "description": "A Chrome extension for text highlighting",
  "main": "background.js",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand --cacheDirectory=./.jest_cache",
    "deploy": "node ./scripts/deploy.cjs",
    "deploy:firefox": "node ./scripts/deploy.cjs firefox",
    "version-deploy": "node ./scripts/version-deploy.cjs"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "archiver": "^7.0.1",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.52.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "transform": {},
    "setupFilesAfterEnv": ["<rootDir>/tests/setup.js"],
    "testPathIgnorePatterns": [
      "<rootDir>/e2e-tests/"
    ]
  }
}
